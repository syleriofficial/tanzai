"use client";import { useState } from "react";import Sidebar from "@/components/Sidebar";import { UploadCloud } from "lucide-react";
export default function Files(){const [msg,setMsg]=useState("Upload UI + API stub ready.");async function upload(e){const file=e.target.files?.[0];if(!file)return;const fd=new FormData();fd.append("file",file);const r=await fetch("/api/upload",{method:"POST",body:fd});const d=await r.json();setMsg(d.message+" File: "+d.filename)}return <div className="shell"><Sidebar/><main className="main"><div className="top"><div><b>Files</b><br/><span className="small">{msg}</span></div></div><div className="card upload"><UploadCloud size={42}/><h2>Upload file</h2><p className="small">V11 API stub. V12 can connect Firebase Storage / Google Cloud Storage.</p><input className="input" type="file" onChange={upload}/></div></main></div>}
