"use client";import Navbar from "@/components/Navbar";import Footer from "@/components/Footer";
const plans=[["Free","$0",["Limited chat","Basic tools","Community support"]],["Pro","$9",["More messages","Premium tools","Fast responses"]],["Ultra","$19",["Advanced models","Higher limits","Priority features"]],["Teams","$49",["Team workspace","Admin controls","Shared AI tools"]]];
export default function Pricing(){async function checkout(plan){const r=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({plan})});const d=await r.json();if(d.url)location.href=d.url;else alert(d.error||"Stripe checkout not configured yet.")}return <div className="bg"><Navbar/><section className="section"><h1 style={{fontSize:56}}>Pricing</h1><p className="lead">Stripe checkout API placeholder is included in V11.</p><div className="grid4 mt">{plans.map((p,i)=><div className="card" key={p[0]}><h3>{p[0]}</h3><div className="price">{p[1]}</div><div>{p[2].map(x=><p key={x}>✓ {x}</p>)}</div><button className={i==1?"btn primary full":"btn full"} onClick={()=>i===0?location.href="/signup":checkout(p[0].toLowerCase())}>{i===0?"Start":"Choose"}</button></div>)}</div></section><Footer/></div>}
