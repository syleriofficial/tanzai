"use client";import {useState} from "react";export default function Home(){const[prompt,setPrompt]=useState("");const[reply,setReply]=useState("");async function askAI(){setReply("Syleri Engine is thinking...");const response=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages:[{role:"user",content:prompt}]})});const data=await response.json();setReply(data.reply||"No response")}return <main className="main"><div className="title">Tanzai AI</div><div className="subtitle">Powered by Syleri Engine • Built by Syleri</div><div className="box"><textarea placeholder="Message Tanzai AI..." value={prompt} onChange={(e)=>setPrompt(e.target.value)} /><button onClick={askAI}>Ask Tanzai AI</button>{reply&&<div className="reply">{reply}</div>}</div></main>}